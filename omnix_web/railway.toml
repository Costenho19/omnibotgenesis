[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn api.server:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
