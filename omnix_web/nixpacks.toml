[phases.setup]
nixPkgs = ["python311", "nodejs_20", "python311Packages.pip"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "npm install"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "gunicorn api.server:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
