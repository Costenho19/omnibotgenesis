# Nixpacks configuration for Railway deployment
# OMNIX Telegram Bot - Python 3.11

providers = ["python"]

[phases.setup]
nixPkgs = ["gcc", "libffi", "openssl"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "python -u main.py"
