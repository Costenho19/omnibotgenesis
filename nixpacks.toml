# Nixpacks configuration — omnibotgenesis Telegram Bot
# nixpacks auto-detects Python from requirements.txt and handles pip.
# Only the start command is forced here to prevent Procfile conflict.

[start]
cmd = "python3 -u main.py"
