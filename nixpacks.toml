# Nixpacks configuration — omnibotgenesis Telegram Bot
# start command is defined in railway.json: "python -u main.py"

[variables]
NIXPACKS_PYTHON_VERSION = "3.12"

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]
